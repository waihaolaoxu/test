"use client";

export default function Page({ params }) {
    return (
        <>
            {JSON.stringify(params)}
            <div>
                "aa/bb/[id]/page.js"
            </div>
        </>
    )
}